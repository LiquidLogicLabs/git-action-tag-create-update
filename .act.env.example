# Act Environment Variables for git-create-update-tag-action
# These variables are used when running act locally
# 
# Note: If you have secrets configured in ~/.actrc, you can leave these commented
# and act will use the values from ~/.actrc instead
#
# Copy this file to .act.env and uncomment/modify values as needed

# GitHub Actions environment variables (automatically set by act, but can override)
# GITHUB_ACTOR=your-username
# GITHUB_REPOSITORY=LiquidLogicLabs/git-create-update-tag-action
# GITHUB_REF=refs/heads/main
# GITHUB_SHA=your-commit-sha

# Optional: Override default GitHub API URL (for testing GitHub Enterprise or custom endpoints)
# GITHUB_API_URL=https://api.github.com

# Optional: Override default Gitea API URL (for testing self-hosted instances)
# GITEA_API_URL=https://git.ravenwolf.org/api/v1

# Optional: Override default Bitbucket API URL (for testing Bitbucket Server)
# BITBUCKET_API_URL=https://api.bitbucket.org/2.0

