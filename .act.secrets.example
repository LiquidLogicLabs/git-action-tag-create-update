# Act secrets for local E2E runs with act
# Copy to .act.secrets and uncomment/set what you need. Never commit.

# GitHub E2E token (PAT with repo write)
# TEST_GITHUB_TOKEN=your-test-github-token

# Optional Gitea / Bitbucket tokens (only if testing those)
# TEST_GITEA_TOKEN=your_gitea_token_here
# TEST_BITBUCKET_TOKEN=your_bitbucket_token_here

# NPM Registry URL (required for CI workflows)
# URL of the npm registry where git-platform-detector is published
# NPM_REGISTRY_URL=https://git.ravenwolf.org/api/packages/liquidlogiclabs/npm

# NPM Registry Token (required for CI workflows)
# Token for authenticating with the npm registry
# Set this to your Gitea token if using Gitea's npm registry
# NPM_REGISTRY_TOKEN=

# Gitea PAT for npm registry authentication (required for CI workflows)
# This is used by CI workflows to authenticate with Gitea npm registry
# Set this to the same value as TEST_GITEA_TOKEN if using the same token
# GITEA_TOKEN=your-gitea-token

